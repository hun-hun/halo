<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.halo.dao.BookDao">

    <resultMap id="BaseResultMap" type="com.halo.entity.Book">
        <result column="id" property="id" />
        <result column="name" property="name" />
        <result column="author" property="author" />
        <result column="price" property="price" />
    </resultMap>

    <insert id="insert" parameterType="com.halo.entity.Book">
        insert into book (id, name, author, price) values
        ( #{id, jdbcType="INTEGER"}, #{name, jdbcType="VARCHAR"}, {author, jdbcType="VARCHAR"}, #{price, jdbcType="DOUBLE"} )
    </insert>

    <select id="selectAll" resultMap="BaseResultMap">
        select id, name, author, price
        from book
    </select>

    <select id="getById" resultMap="BaseResultMap">
        select id, name, author, price
        from book
        WHERE id = #{id}
     </select>

</mapper>
